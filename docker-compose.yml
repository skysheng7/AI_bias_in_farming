services:
  analysis-env:
    image: skysheng7/ai_bias:3c968bc
    ports:
      - "8888:8888"
    volumes:
      - .:/home/jovyan
    env_file:
      - .env
    deploy:
      resources:
        limits:
          memory: 5G
    platform: linux/amd64
